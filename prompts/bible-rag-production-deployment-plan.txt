```text
I have a working Bible-RAG project with:

- FastAPI backend
- React + Vite frontend
- FAISS vector retrieval
- OpenAI generation
- JWT authentication
- retrieval strategy selector
- local development workflow

The project currently works locally using:

python3 run_dev.py

I now want to prepare the project for PUBLIC DEPLOYMENT while still fully supporting LOCAL DEVELOPMENT.

Please carefully refactor and productionize the project without breaking the current local workflow.

========================================
MAIN GOALS
========================================

1. Keep local development simple and working.
2. Prepare backend for cloud deployment.
3. Prepare frontend for Vercel deployment.
4. Secure secrets and environment variables properly.
5. Make deployment reproducible.
6. Improve production readiness.
7. Keep the architecture clean and maintainable.

========================================
IMPORTANT REQUIREMENTS
========================================

DO NOT break:
- local FastAPI development
- local React/Vite development
- run_dev.py
- current RAG functionality
- authentication flow

The app must continue working locally exactly as before.

========================================
PART 1 — ENVIRONMENT CONFIGURATION
========================================

Implement clean environment handling.

Backend should support:
- local .env
- cloud environment variables

Frontend should support:
- local .env.local
- Vercel environment variables

========================================
BACKEND ENV VARIABLES
========================================

Backend .env example:

OPENAI_API_KEY=sk-...
APP_AUTH_PASSWORD=change-me
JWT_SECRET=super-secret-key
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,https://my-frontend.vercel.app
ENVIRONMENT=development

Requirements:
- use python-dotenv
- never hardcode secrets
- never commit .env
- add .env.example
- validate required env vars
- clear startup errors if missing

========================================
FRONTEND ENV VARIABLES
========================================

Frontend should use:

VITE_API_BASE_URL

Example local:

VITE_API_BASE_URL=http://localhost:8000

Example production:

VITE_API_BASE_URL=https://my-backend.onrender.com

Use:
import.meta.env.VITE_API_BASE_URL

Do NOT hardcode localhost URLs anywhere.

========================================
PART 2 — GITIGNORE CLEANUP
========================================

Ensure .gitignore includes:

Backend:
- .env
- __pycache__/
- *.pyc

Frontend:
- node_modules/
- dist/
- .env.local

General:
- .DS_Store
- .idea/
- .vscode/

Do NOT ignore:
- package-lock.json
- requirements.txt

========================================
PART 3 — BACKEND PRODUCTIONIZATION
========================================

Prepare FastAPI backend for deployment.

Requirements:
- production-ready uvicorn command
- proper CORS handling
- startup validation
- graceful errors
- no stack traces leaked to clients
- health endpoint

Required endpoint:
GET /health

Response:
{
  "status": "ok"
}

========================================
CORS REQUIREMENTS
========================================

Development:
allow localhost ports.

Production:
restrict to configured frontend domain(s).

Use ALLOWED_ORIGINS env variable.

========================================
PART 4 — MODEL / INDEX LOADING
========================================

Ensure:
- embedding model loads once
- FAISS index loads once
- chunks load once

Do NOT reload model/index per request.

Use FastAPI lifespan or singleton pattern.

========================================
PART 5 — DOCKER SUPPORT
========================================

Add Docker support for backend.

Create:
backend/Dockerfile

Requirements:
- Python slim image
- install requirements
- expose PORT
- production uvicorn command

Should work on:
- Render
- Railway
- Fly.io
- Cloud Run

Optional:
- docker-compose for local dev

========================================
PART 6 — FRONTEND DEPLOYMENT PREPARATION
========================================

Prepare React/Vite frontend for Vercel deployment.

Requirements:
- no backend secrets in frontend
- environment-based API URL
- production build works
- no localhost assumptions

Add:
frontend/vercel.json if useful.

========================================
PART 7 — AUTHENTICATION HARDENING
========================================

Ensure:
- JWT validation robust
- expired tokens handled
- unauthorized requests return 401
- frontend redirects to login on 401

Do NOT expose:
- OpenAI API key
- JWT secret
- auth password

========================================
PART 8 — RATE LIMITING
========================================

Add lightweight rate limiting.

Suggested:
- 10 requests/minute per IP or token
- configurable via env

Return:
429 Too Many Requests

========================================
PART 9 — PRODUCTION LOGGING
========================================

Improve logging.

Requirements:
- structured logs
- startup logs
- retrieval logs
- generation logs
- error logs

Do NOT log:
- secrets
- JWT tokens
- OpenAI keys

========================================
PART 10 — LOCAL DEVELOPMENT SUPPORT
========================================

Preserve current local workflow.

run_dev.py should still:
- launch backend
- launch frontend
- support hot reload

Handle port conflicts gracefully.

Frontend local:
http://localhost:5173

Backend local:
http://localhost:8000

========================================
PART 11 — DEPLOYMENT DOCUMENTATION
========================================

Update README with:

========================================
LOCAL DEVELOPMENT
========================================

Backend:
pip install -r requirements.txt

Frontend:
npm install

Run:
python3 run_dev.py

========================================
LOCAL ENVIRONMENT VARIABLES
========================================

Backend .env example
Frontend .env.local example

========================================
BACKEND DEPLOYMENT
========================================

Deploy to:
- Render
OR
- Railway

Include:
- build command
- start command
- required env vars

========================================
FRONTEND DEPLOYMENT
========================================

Deploy to:
- Vercel

Include:
- environment variables
- API URL configuration

========================================
CORS CONFIGURATION
========================================

Explain how frontend/backend domains communicate.

========================================
PART 12 — HEALTH-CHECKS
========================================

Add health endpoints useful for deployment platforms.

GET /health

Optionally:
GET /health/detailed

Should verify:
- index loaded
- model loaded
- OpenAI configured

========================================
PART 13 — ERROR HANDLING
========================================

Frontend should gracefully handle:
- backend offline
- timeout
- invalid token
- rate limit
- OpenAI quota exceeded

Show user-friendly Hebrew messages.

========================================
PART 14 — SECURITY
========================================

Ensure:
- no secrets committed
- no secrets exposed to frontend
- secure JWT handling
- proper environment separation
- production-safe defaults

========================================
PART 15 — PRE-DEPLOYMENT CHECKLIST
========================================

Create a deployment checklist Markdown file.

Include:
- env vars configured
- OpenAI quota enabled
- backend deployed
- frontend deployed
- CORS configured
- authentication tested
- health endpoint tested
- rate limiting tested

========================================
IMPORTANT DESIGN PRINCIPLES
========================================

The project should now feel like:
- a serious AI web application
- production-aware
- cloud deployable
- still easy to run locally
- clean and maintainable

Do NOT overengineer.
Keep it practical and robust.

Please implement step-by-step and explain major architectural changes.
```
